@model IEnumerable<ProySimulacro1.Models.Language>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.iso639_2)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.nativeName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.iso639_2)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.nativeName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.iso639_1 }) |
            @Html.ActionLink("Details", "Details", new { id=item.iso639_1 }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.iso639_1 })
        </td>
    </tr>
}

</table>
